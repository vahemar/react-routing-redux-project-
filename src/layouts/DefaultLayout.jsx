import { Flex } from '@chakra-ui/react';
import React from 'react';

const DefaultLayout = () => {
    return (
        <Flex>
            DefaultLayout
        </Flex>
    );
}

export default DefaultLayout;
